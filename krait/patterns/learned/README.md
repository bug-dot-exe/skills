# Learned Patterns — Archive Only

These YAML files are **historical documentation**, not detection input.

All lessons from shadow audits are integrated directly into the detection methodology:
- `.claude/skills/krait-detector/SKILL.md` — heuristics, questions, modules
- `.claude/commands/krait.md` — the main audit command

The `archive/` subdirectory contains raw analysis from past shadow audits for reference.
New lessons should be added as skill modifications, not YAML files.
