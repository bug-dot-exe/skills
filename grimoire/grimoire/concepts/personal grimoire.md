
Your personal grimoire is a directory that lives at `~/.grimoire/` which contains a couple of useful things.

Currently the grimoire primarily contains a `sigils` directory which is managed through the [[skills/scribe|scribe]] skill.